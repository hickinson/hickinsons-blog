---
title: "Post title here"
description: "One clear sentence explaining what the post is about."
post_date: "2026-01-01"
post_category: "other"
image: "/og-default.png"
---

export { MDXLayout as default } from '../components/MDXLayout';
import { SEO } from "../components/SEO"

export const Head = (props) => (
  <SEO
    frontmatter={props?.pageContext?.frontmatter || {}}
    pathname={props?.location?.pathname || ''}
  />
);

{/*
Before drafting, read EDITORIAL.md and choose the form the material deserves.
Do not assume the post needs headings, lessons, a universal conclusion or a neat closing line.
Replace the placeholder frontmatter above before publishing.
*/}

Start writing here.
