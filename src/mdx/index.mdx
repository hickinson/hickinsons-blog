---
post_category: non_blog_post
post_date: 2023-09-11
---

export { Layout as default } from '../components/Layout';
import { SEO } from "../components/SEO"
import { Button } from "../components/Button"
import { Section } from "../components/Section"
import PostList from '../components/PostList';

export const Head = (props) => (
  <SEO frontmatter={props?.pageContext?.frontmatter || {}} />
);

<Section>
  <div className="max-w-[46rem] pt-2 md:pt-4">
    <p className="mb-3 text-[0.78rem] font-semibold uppercase tracking-[0.14em] text-site-muted">
      hickinsons.blog
    </p>

    <h1 className="mb-6">
      Hi, I’m Phil.
    </h1>

    <p className="max-w-reading text-[1.08rem] leading-[1.9] text-site-muted mb-8">
      I write about data and digital transformation, leadership, systems, entrepreneurship, and the discipline required to build things that last.
    </p>

    <div className="flex flex-wrap gap-3">
      <Button to="/about">About Phil</Button>
      <Button href="#latest-writing">Latest writing</Button>
    </div>
  </div>
</Section>

<Section>
  <div id="latest-writing" className="max-w-[52rem] scroll-mt-24">
    <p className="mb-3 text-[0.78rem] font-semibold uppercase tracking-[0.14em] text-site-muted">
      Writing
    </p>

    <h2 className="mb-4">
      Latest writing
    </h2>

    <p className="max-w-reading text-site-muted mb-8">
      Notes, essays and working thoughts on data, leadership, systems, building, and life.
    </p>

    <PostList />
  </div>
</Section>